//created a seed module with function to return an object with type, height and output
//countine with each plant.

export const createAsparagus = () => {
    const plantProp = {
        type: "Asparagus",
        height: 24,
        output: 4
    }
    return plantProp
}